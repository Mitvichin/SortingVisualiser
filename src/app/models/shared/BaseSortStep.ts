import { ComparedCouple } from './ComparedCouple';

export class BaseSortClass{
    comparedCouple: ComparedCouple;
    didSwap: boolean;
    startingArr: number[];
    resultArr: number[];
    isCompleted: boolean
}